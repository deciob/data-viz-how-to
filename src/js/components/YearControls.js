/* eslint-disable no-unused-vars */
import React from 'react';
/* eslint-enable no-unused-vars */

const YearControls = (first, last, current) => (
  <div className="year-controls">
    these are the year controls!
  </div>
);

export default YearControls;
